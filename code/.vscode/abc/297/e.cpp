#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
int main() {
    int n, k;
    cin >> n >> k;
    
    set<long long> A;
    long long a;
    rep(i, n){
        cin >> a;
        A.insert(a);
    }

    while(k-- > 0){
        
    }
}